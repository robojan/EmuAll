
namespace Shaders {
	unsigned int LoadShaderMem(const char *vertex, int vertexSize, const char *fragment, int fragmentSize);
	unsigned int LoadVertexShaderMem(const char *vertex, int vertexSize);
	unsigned int LoadFragmentShaderMem(const char *fragment, int fragmentSize);
}
